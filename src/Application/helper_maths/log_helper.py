

def swap_row_log(index1, index2):
  return "Se cambia la fila " + str(index2+1) +" por la fila " + str(index1+1)

def swap_col_log(index1, index2):
  return "Se cambia la columna " + str(index2+1) +" por la columna " + str(index1+1)

def current_pivot_log(index):
  return "Pivote en la fila " + str(index+1)